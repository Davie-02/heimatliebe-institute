# Heimatliebe Institute platform — runtime dependencies (pinned for reproducible deploys)
fastapi==0.141.1
uvicorn[standard]==0.53.0      # includes uvloop + httptools for fast async networking
sqlalchemy[asyncio]==2.1.0
asyncpg==0.31.0                # PostgreSQL driver (production)
aiosqlite==0.22.1              # SQLite driver (development and staging)
bcrypt==5.0.0
PyJWT==2.15.0
orjson==3.12.0
aiosmtplib==5.1.3
httpx==0.28.1
python-multipart==0.0.32
Pillow==12.3.0                 # shrinks uploaded photos for slow connections
# Optional: install and set REDIS_URL to share realtime events between several server processes.
# redis==6.4.0
