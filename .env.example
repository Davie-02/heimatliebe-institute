# Website (Vite) settings for local development — copy to `.env.local`.
# On Vercel nothing is needed for production: the site calls /api on its own address and
# vercel.json forwards it to the Render API.
VITE_API_BASE_URL=http://localhost:3001/api
